package com.challa.codeahead1.array;
//find index where left elements are less and all right elements are greater
public class FindPivot {
    
    public static void main(String[] args) {
       int   arr[] = {5, 1, 4, 3, 6, 8, 10, 7, 9};
    }

    private static int findPivotIndex(int arr[]) {
        int i=0;
        while(i<arr.length){
           int  j=i;
            while(j<arr.length) {
                if(arr[i]<arr[j]) {
                }
            }
        }
        return -1;
    }
}
